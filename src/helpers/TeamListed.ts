export default class TeamListed {
  id: number;
  name: string;
  country: string;

  constructor(id: number, name: string, country: string) {
    this.id = id;
    this.name = name;
    this.country;
  }
}
